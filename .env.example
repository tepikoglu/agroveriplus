# AgroVeri+ Docker Compose Environment
# Copy to .env and adjust values

# Frontend (Nginx)
APP_PORT=8080

# Database
DB_USER=agroveri
DB_PASSWORD=agroveri

# Backend (see backend/.env.example for full list)
JWT_SECRET=change-this-to-a-random-secret
IPFS_API_KEY=
IPFS_API_SECRET=
